#include <stdio.h>
#include "temperature_conversor.h"

int main()
{
    float temperatura, convertido;
    char from, to;
    scanf("%f %c %c", &temperatura, &from, &to);
    printf("Temperature: %f", convertido);
    return 0;
}